# design-sync notes — PlayBlazor UX Concepts

- **This is NOT a design-system import.** The synced content is the set of interactive UX
  mockups in `docs/superpowers/design/2026-08-28-playblazor-ux-concepts/` (PlayBlazor shell
  concepts, rounds 1–3). No dist build, no Storybook, no React components — the converter
  pipeline does not apply. `_ds_sync.json` is deliberately omitted (off-script generation:
  no anchor, every future sync just re-uploads, which is correct at this size).
- **Bundle layout** (`ds-bundle/`, untracked, regenerated by
  `scratchpad/build_ds_bundle.py` logic): `mockups/<page>.html` — each page is the repo
  source with `round2-model.js` INLINED (pages d–g reference it via `<script src>`, which
  would not resolve as a project card) and a `<!-- @dsCard group="…" -->` first line.
  Groups: "Start here" (hub), "Round 1 · Exploration" (a/b/c), "Round 2 · Scale" (d/e/f),
  "Round 3 · Synthesis" (g — the retained direction).
- **Re-sync recipe**: regenerate ds-bundle (inline the model, prepend @dsCard), then
  finalize_plan(localDir ds-bundle, writes ["mockups/**","README.md"], deletes
  ["mockups/**"]) and write_files with localPath.
- Concept B links Google Fonts (Archivo, IBM Plex Mono) with full system fallbacks — if the
  design pane blocks external fonts the card still renders correctly.
